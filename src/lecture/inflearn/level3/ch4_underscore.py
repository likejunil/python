"""
파이썬에서는 3가지 경우에 underscore 를 사용한다.
1. 값을 무시
    # first, *_, last = [1, 2, 3, 4]
    # for _ in range(1, 11):

2. 접근지정자 (강제성은 없지만 전 세계의 파이썬 프로그래머들이 이 규칙을 따르고 있다.)
    # {var} public
    # _{var} protected
    # __{var} private

3. 인터프리터

"""